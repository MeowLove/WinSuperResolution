<Window x:Class="WinSuperResolution.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Ui[ProductName]}" Icon="Assets/WinSuperResolution.ico" Height="900" Width="1440" MinHeight="720" MinWidth="1024"
        WindowStartupLocation="CenterScreen" Background="#F4F6F8" FontFamily="Segoe UI" UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Window.Resources>
        <SolidColorBrush x:Key="InkBrush" Color="#17212B" />
        <SolidColorBrush x:Key="MutedBrush" Color="#5D6B78" />
        <SolidColorBrush x:Key="AccentBrush" Color="#087E8B" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#056773" />
        <SolidColorBrush x:Key="OutlineBrush" Color="#D8E0E6" />
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFF" />

        <Style TargetType="Button">
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#AEBBC4" />
                                <Setter Property="Foreground" Value="#EEF2F4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="QuietButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#E8EEF1" />
            <Setter Property="Foreground" Value="#24404A" />
        </Style>
        <Style x:Key="HeaderButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#24525A" />
            <Setter Property="Foreground" Value="#F7FCFC" />
        </Style>
        <Style x:Key="DangerHeaderButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#8F3F46" />
            <Setter Property="Foreground" Value="#FFF8F8" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#C9D5DB" />
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="BorderBrush" Value="#E8EDF0" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F8FA" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#D8EEF2" />
                    <Setter Property="Foreground" Value="#123C46" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DataGridTextElement" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#E8EDF0" />
            <Setter Property="VerticalGridLinesBrush" Value="Transparent" />
            <Setter Property="RowHeight" Value="46" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="AlternationCount" Value="2" />
            <Setter Property="AlternatingRowBackground" Value="#FAFCFD" />
            <Setter Property="ColumnHeaderHeight" Value="42" />
            <Setter Property="EnableRowVirtualization" Value="True" />
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#EEF3F5" />
            <Setter Property="Foreground" Value="#51616E" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,9" />
            <Setter Property="BorderBrush" Value="#DCE5E9" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0,0,0,1" Padding="{TemplateBinding Padding}">
                            <DockPanel LastChildFill="True">
                                <TextBlock x:Name="SortGlyph" DockPanel.Dock="Right" Margin="8,0,0,0" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" />
                                <ContentPresenter VerticalAlignment="Center" />
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="SortDirection" Value="Ascending">
                                <Setter TargetName="SortGlyph" Property="Text" Value="▲" />
                            </Trigger>
                            <Trigger Property="SortDirection" Value="Descending">
                                <Setter TargetName="SortGlyph" Property="Text" Value="▼" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="18,18,18,14" Padding="24,20" Background="#143D43" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{Binding Ui[ProductName]}" Foreground="White" FontSize="32" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding Ui[Subtitle]}" Foreground="#C8DBDE" FontSize="15" Margin="0,5,0,16" TextWrapping="Wrap" />
                    <StackPanel Orientation="Horizontal">
                        <Button Content="{Binding Ui[Refresh]}" Style="{StaticResource HeaderButton}" Click="RefreshButton_Click" />
                        <Button Content="{Binding Ui[DisplaySettings]}" Style="{StaticResource HeaderButton}" Click="DisplaySettingsButton_Click" />
                        <Button Content="{Binding Ui[ExportDiagnostic]}" Style="{StaticResource HeaderButton}" Click="ExportDiagnosticButton_Click" />
                        <Button Content="{Binding Ui[ResetDisplayCache]}" Style="{StaticResource DangerHeaderButton}" Click="ResetDisplayCacheButton_Click" />
                        <Button Content="{Binding Ui[AboutAuthor]}" Style="{StaticResource HeaderButton}" Click="AboutAuthorButton_Click" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Top" Width="150">
                    <TextBlock Text="{Binding Ui[Language]}" Foreground="#C8DBDE" Margin="0,0,0,5" />
                    <ComboBox ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="18,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.05*" MinWidth="480" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="1*" MinWidth="430" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="12" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource OutlineBrush}" BorderThickness="1" CornerRadius="7" Padding="16">
                    <StackPanel>
                        <DockPanel LastChildFill="False">
                            <TextBlock Text="{Binding Ui[Compatibility]}" Foreground="{StaticResource InkBrush}" FontSize="17" FontWeight="SemiBold" />
                            <Border DockPanel.Dock="Right" Background="{Binding CompatibilityStatusBackground}" CornerRadius="4" Padding="8,3" Margin="12,0,0,0">
                                <TextBlock Text="{Binding CompatibilityStatusText}" Foreground="{Binding CompatibilityStatusForeground}" FontWeight="SemiBold" />
                            </Border>
                        </DockPanel>
                        <TextBlock Text="{Binding CompatibilitySummary}" Foreground="{StaticResource InkBrush}" TextWrapping="Wrap" Margin="0,8,0,8" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{Binding Ui[CompatibilitySystem]}" Foreground="{StaticResource MutedBrush}" />
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding CompatibilitySystem}" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis" ToolTip="{Binding CompatibilitySystem}" />
                            <TextBlock Grid.Row="1" Text="{Binding Ui[CompatibilityProcessor]}" Foreground="{StaticResource MutedBrush}" Margin="0,3,0,0" />
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding CompatibilityProcessor}" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis" ToolTip="{Binding CompatibilityProcessor}" Margin="0,3,0,0" />
                            <TextBlock Grid.Row="2" Text="{Binding Ui[CompatibilityGraphics]}" Foreground="{StaticResource MutedBrush}" Margin="0,3,0,0" />
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding CompatibilityGraphics}" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis" ToolTip="{Binding CompatibilityGraphics}" Margin="0,3,0,0" />
                        </Grid>
                        <Expander Header="{Binding Ui[CompatibilityDetails]}" Foreground="{StaticResource AccentBrush}" Margin="0,8,0,0">
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="{Binding Ui[CompatibilitySelectedDisplay]}" Foreground="{StaticResource MutedBrush}" />
                                <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding CompatibilitySelectedDisplay}" Foreground="{StaticResource InkBrush}" TextWrapping="Wrap" />
                                <TextBlock Grid.Row="1" Text="{Binding Ui[CompatibilityDisplayPath]}" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                                <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding CompatibilityDisplayPath}" Foreground="{StaticResource InkBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />
                                <TextBlock Grid.Row="2" Text="{Binding Ui[CompatibilityAdapterAssociation]}" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                                <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding CompatibilityAdapterAssociation}" Foreground="{StaticResource InkBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />
                                <TextBlock Grid.Row="3" Text="{Binding Ui[CompatibilityDriverFreshness]}" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                                <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding CompatibilityDriverFreshness}" Foreground="{StaticResource InkBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />
                                <TextBlock Grid.Row="4" Text="{Binding Ui[CompatibilityOtherGraphics]}" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                                <TextBlock Grid.Row="4" Grid.Column="2" Text="{Binding CompatibilityOtherGraphics}" Foreground="{StaticResource InkBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />
                            </Grid>
                        </Expander>
                    </StackPanel>
                </Border>
                <Border Grid.Row="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource OutlineBrush}" BorderThickness="1" CornerRadius="7">
                    <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{Binding Ui[RegisteredConfigurations]}" Foreground="{StaticResource InkBrush}" FontSize="17" FontWeight="SemiBold" Margin="16,16,16,4" />
                    <TextBlock Grid.Row="1" Text="{Binding Ui[DisplayRecordsHint]}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="16,0,16,12" />
                    <DataGrid x:Name="DisplayGrid" Grid.Row="2" ItemsSource="{Binding Records}" SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                              AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" CanUserAddRows="False" CanUserSortColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="" Binding="{Binding ConnectionStatusText}" SortMemberPath="ConnectionStatus" CanUserSort="True" Width="0.7*" ElementStyle="{StaticResource DataGridTextElement}" />
                            <DataGridTextColumn Header="" Binding="{Binding DisplayIdentity}" SortMemberPath="DisplayIdentity" CanUserSort="True" Width="2.5*" ElementStyle="{StaticResource DataGridTextElement}" />
                            <DataGridTextColumn Header="" Binding="{Binding PrimarySurfaceDisplayText}" SortMemberPath="PrimarySurfaceWidth" CanUserSort="True" Width="1.25*" ElementStyle="{StaticResource DataGridTextElement}" />
                            <DataGridTextColumn Header="" Binding="{Binding ActiveSignalDisplayText}" SortMemberPath="ActiveSignalWidth" CanUserSort="True" Width="1.25*" ElementStyle="{StaticResource DataGridTextElement}" />
                            <DataGridTextColumn Header="" Binding="{Binding MatchStatusText}" SortMemberPath="MatchStatus" CanUserSort="True" Width="0.95*" ElementStyle="{StaticResource DataGridTextElement}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
                </Border>
            </Grid>

            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource OutlineBrush}" BorderThickness="1" CornerRadius="7" Padding="18" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="{Binding Ui[CurrentState]}" Foreground="{StaticResource InkBrush}" FontSize="17" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding CurrentStateSummary}" Foreground="{StaticResource InkBrush}" TextWrapping="Wrap" Margin="0,8,0,4" />
                            <TextBlock Text="{Binding Ui[CurrentStateGuard]}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" />
                            <Border Background="#FFF7ED" BorderBrush="#F1C48E" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,12,0,0">
                                <StackPanel>
                                    <TextBlock Text="{Binding Ui[ExperimentalScale]}" Foreground="#8A4A0B" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding ScaleAvailability}" Foreground="#704015" TextWrapping="Wrap" Margin="0,4,0,8" />
                                    <WrapPanel>
                                        <ComboBox Width="105" ItemsSource="{Binding AvailableScalePercentages}" SelectedItem="{Binding SelectedScalePercent}" IsEnabled="{Binding CanApplyExperimentalScale}" />
                                        <Button Content="{Binding Ui[ApplyScale]}" Click="ApplyScaleButton_Click" IsEnabled="{Binding CanApplyExperimentalScale}" />
                                        <Button Content="{Binding Ui[RestoreScale]}" Style="{StaticResource QuietButton}" Click="RestoreScaleButton_Click" />
                                    </WrapPanel>
                                </StackPanel>
                            </Border>

                        </StackPanel>
                    </Border>

                    <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource OutlineBrush}" BorderThickness="1" CornerRadius="7" Padding="18" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="{Binding Ui[CapabilityPlan]}" Foreground="{StaticResource InkBrush}" FontSize="17" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding SelectedSummary}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,8,0,12" />
                            <StackPanel Margin="0,0,0,10">
                                <DockPanel>
                                    <TextBlock Text="{Binding Ui[Magnification]}" VerticalAlignment="Center" Foreground="{StaticResource InkBrush}" />
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding SelectedMagnification, StringFormat={}{0}%}" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" />
                                </DockPanel>
                                <Slider Minimum="100" Maximum="350" TickFrequency="10" IsSnapToTickEnabled="True" TickPlacement="BottomRight" Value="{Binding SelectedMagnification, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,0" />
                                <WrapPanel Margin="0,5,0,0">
                                    <Button Content="120%" Tag="120" Style="{StaticResource QuietButton}" Click="SetMagnificationButton_Click" />
                                    <Button Content="150%" Tag="150" Style="{StaticResource QuietButton}" Click="SetMagnificationButton_Click" />
                                    <Button Content="200%" Tag="200" Style="{StaticResource QuietButton}" Click="SetMagnificationButton_Click" />
                                    <Button Content="250%" Tag="250" Style="{StaticResource QuietButton}" Click="SetMagnificationButton_Click" />
                                    <Button Content="{Binding Ui[BuildPlan]}" Click="BuildPlanButton_Click" />
                                </WrapPanel>
                            </StackPanel>
                            <TextBlock Text="{Binding PlanSummary}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" />
                            <TextBlock Text="{Binding Ui[AvailableModes]}" Foreground="{StaticResource InkBrush}" FontWeight="SemiBold" Margin="0,16,0,6" />
                            <DockPanel>
                                <Button DockPanel.Dock="Right" Content="{Binding Ui[ApplyMode]}" Click="ApplyModeButton_Click" IsEnabled="{Binding CanManageCurrentState}" Margin="8,0,0,0" />
                                <ComboBox ItemsSource="{Binding CurrentModes}" SelectedItem="{Binding SelectedMode}" DisplayMemberPath="DisplayText" IsEnabled="{Binding CanManageCurrentState}" />
                            </DockPanel>
                            <TextBlock Text="{Binding CurrentModeAvailability}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,6,0,0" />
                            <WrapPanel Margin="0,14,0,0">
                                <Button Content="{Binding Ui[ApplySelected]}" Click="ApplySelectedButton_Click" IsEnabled="{Binding CanApplySelectedCapability}" />
                                <Button Content="{Binding Ui[ApplyAll]}" Style="{StaticResource QuietButton}" Click="ApplyAllButton_Click" IsEnabled="{Binding CanApplyAllCapabilities}" />
                                <Button Content="{Binding Ui[RestoreLatest]}" Style="{StaticResource QuietButton}" Click="RestoreLatestButton_Click" />
                            </WrapPanel>
                            <Border Background="#F1F7F8" BorderBrush="#BFD9DE" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,14,0,0">
                                <TextBlock Text="{Binding Ui[CapabilityWarning]}" Foreground="#315A62" TextWrapping="Wrap" />
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource OutlineBrush}" BorderThickness="1" CornerRadius="7" Padding="18">
                        <StackPanel>
                            <TextBlock Text="{Binding Ui[Recovery]}" Foreground="{StaticResource InkBrush}" FontSize="17" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Ui[RecoveryLocation]}" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,2" />
                            <TextBlock Text="{Binding RecoveryDataPath}" ToolTip="{Binding RecoveryDataPath}" Foreground="#245A65" TextTrimming="CharacterEllipsis" />
                            <Button Content="{Binding Ui[OpenRecoveryFolder]}" Style="{StaticResource QuietButton}" Click="OpenRecoveryFolderButton_Click" HorizontalAlignment="Left" Margin="0,10,0,10" />
                            <Separator Background="#E4EAED" />
                            <TextBlock Text="{Binding LastOperationSummary}" Foreground="{StaticResource InkBrush}" TextWrapping="Wrap" Margin="0,10,0,0" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Border Grid.Row="2" Background="#E7ECEF" Margin="18,14,18,18" Padding="10,7" CornerRadius="5">
            <TextBlock Text="{Binding StatusText}" Foreground="#41525D" TextTrimming="CharacterEllipsis" />
        </Border>
    </Grid>
</Window>
