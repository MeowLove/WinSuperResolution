<Window x:Class="WinSuperResolution.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Ui[ProductName]}" Height="780" Width="1220" MinHeight="660" MinWidth="980"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="MinHeight" Value="32" />
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,14" />
            <Setter Property="Padding" Value="12" />
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="{Binding Ui[ProductName]}" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="{Binding Ui[Subtitle]}" Foreground="#4B5563" Margin="0,4,0,12" />
                <StackPanel Orientation="Horizontal">
                    <Button Content="{Binding Ui[Refresh]}" Click="RefreshButton_Click" />
                    <Button Content="{Binding Ui[DisplaySettings]}" Click="DisplaySettingsButton_Click" />
                    <Button Content="{Binding Ui[CopyDiagnostic]}" Click="CopyDiagnosticButton_Click" />
                </StackPanel>
            </StackPanel>
            <StackPanel Grid.Column="1" VerticalAlignment="Top">
                <TextBlock Text="{Binding Ui[Language]}" Margin="0,0,0,4" />
                <ComboBox Width="110" ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}" />
            </StackPanel>
        </Grid>

        <GroupBox Header="{Binding Ui[RegisteredConfigurations]}" Grid.Row="1" Grid.Column="0" Margin="0,0,14,0">
            <DataGrid ItemsSource="{Binding Records}" SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{Binding DataContext.Ui[Status], RelativeSource={RelativeSource AncestorType=DataGrid}}" Binding="{Binding ConnectionStatus}" Width="80" />
                    <DataGridTextColumn Header="{Binding DataContext.Ui[Display], RelativeSource={RelativeSource AncestorType=DataGrid}}" Binding="{Binding DisplayIdentity}" Width="*" />
                    <DataGridTextColumn Header="{Binding DataContext.Ui[Surface], RelativeSource={RelativeSource AncestorType=DataGrid}}" Binding="{Binding PrimarySurfaceText}" Width="100" />
                    <DataGridTextColumn Header="{Binding DataContext.Ui[Signal], RelativeSource={RelativeSource AncestorType=DataGrid}}" Binding="{Binding ActiveSignalText}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="{Binding Ui[CapabilityPlan]}">
                    <StackPanel>
                        <TextBlock Text="{Binding SelectedSummary}" TextWrapping="Wrap" />
                        <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                            <TextBlock Text="{Binding Ui[Magnification]}" VerticalAlignment="Center" />
                            <ComboBox Width="96" Margin="8,0,10,0" ItemsSource="{Binding MagnificationOptions}" SelectedItem="{Binding SelectedMagnification}" />
                            <Button Content="{Binding Ui[BuildPlan]}" Click="BuildPlanButton_Click" />
                        </StackPanel>
                        <TextBlock Text="{Binding PlanSummary}" TextWrapping="Wrap" Margin="0,12,0,0" />
                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <Button Content="{Binding Ui[ApplySelected]}" Click="ApplySelectedButton_Click" />
                            <Button Content="{Binding Ui[ApplyAll]}" Click="ApplyAllButton_Click" />
                            <Button Content="{Binding Ui[RestoreLatest]}" Click="RestoreLatestButton_Click" />
                        </StackPanel>
                        <Border Margin="0,12,0,0" Padding="10" Background="#FFF7ED" BorderBrush="#FDBA74" BorderThickness="1">
                            <TextBlock Text="{Binding Ui[CapabilityWarning]}" TextWrapping="Wrap" />
                        </Border>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{Binding Ui[CurrentState]}">
                    <StackPanel>
                        <TextBlock Text="{Binding CurrentStateSummary}" TextWrapping="Wrap" />
                        <TextBlock Text="{Binding Ui[CurrentStateGuard]}" TextWrapping="Wrap" Margin="0,8,0,10" Foreground="#4B5563" />
                        <TextBlock Text="{Binding Ui[AvailableModes]}" FontWeight="SemiBold" />
                        <ComboBox ItemsSource="{Binding CurrentModes}" SelectedItem="{Binding SelectedMode}" DisplayMemberPath="DisplayText" Margin="0,5,0,8" IsEnabled="{Binding CanManageCurrentState}" />
                        <Button Content="{Binding Ui[ApplyMode]}" Click="ApplyModeButton_Click" IsEnabled="{Binding CanManageCurrentState}" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding Ui[ExperimentalScale]}" FontWeight="SemiBold" Margin="0,12,0,4" />
                        <TextBlock Text="{Binding ScaleAvailability}" TextWrapping="Wrap" />
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <ComboBox Width="96" ItemsSource="{Binding AvailableScalePercentages}" SelectedItem="{Binding SelectedScalePercent}" IsEnabled="{Binding CanApplyExperimentalScale}" />
                            <Button Content="{Binding Ui[ApplyScale]}" Click="ApplyScaleButton_Click" IsEnabled="{Binding CanApplyExperimentalScale}" />
                        </StackPanel>
                        <TextBlock Text="{Binding Ui[ScaleUnavailable]}" TextWrapping="Wrap" Foreground="#4B5563" Margin="0,4,0,0" />
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <StatusBar Grid.Row="2" Grid.ColumnSpan="2" Margin="0,16,0,0">
            <StatusBarItem Content="{Binding StatusText}" />
        </StatusBar>
    </Grid>
</Window>
